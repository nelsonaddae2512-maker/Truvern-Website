export const metadata = {
  title: "Upload Vendor Evidence – Truvern",
  description: "Upload files for a vendor.",
};

// NOTE: Keep this a SERVER component to satisfy Next prerender.
// If you later need a real upload form with drag-and-drop, mount a
// small client component from here and guard it behind "use client".

export default async function VendorUploadPage() {
  return (
    <main className="container-page py-8">
      <h1 className="text-2xl font-semibold tracking-tight">Upload Vendor Evidence</h1>
      <p className="mt-2 text-sm text-zinc-500">
        This server-rendered page is a safe placeholder to pass build & prerender checks.
      </p>

      <div className="mt-6 rounded-md border border-zinc-200 dark:border-zinc-800 p-5">
        <p className="text-sm">
          To enable uploads, wire this page to your API route and storage provider.
        </p>
        <ul className="mt-3 list-disc pl-6 text-sm text-zinc-600 dark:text-zinc-400">
          <li>Ensure API routes accept multipart/form-data.</li>
          <li>Use a small client component for the file input if needed.</li>
          <li>Keep this server page as the shell for SEO and stability.</li>
        </ul>
      </div>
    </main>
  );
}